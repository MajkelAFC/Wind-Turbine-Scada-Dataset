[project]
name = "wind-energy-analytics-platform"
version = "0.1.0"
description = "Medallion-architecture ETL pipeline for wind turbine SCADA data"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "apache-airflow>=2.7.1",
    "psycopg2-binary>=2.9",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
